
function calculerMoyenneCotes(){
  let total;
  let nombreCotes;
  let retour = "";

  while(true){

    while(retour == "" || isNaN(Number(retour)) || Number(retour) < 0 || Number(retour) > 20){
  		retour = prompt("Veuillez introduire un nombre entre 0 et 99");
  	}
  }
}
